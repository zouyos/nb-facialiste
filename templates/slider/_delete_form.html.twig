<form method="post" action="{{ path('app_slider_delete', {'id': slider.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette image ?') & setFormSubmitting();">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ slider.id) }}">
	<button class="custom-btn cancel">Supprimer</button>
</form>