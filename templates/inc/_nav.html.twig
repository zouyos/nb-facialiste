<nav id="nav" class="navbar navbar-expand-lg py-2">
	<div class="container-fluid row">
		<img id="logo" class="mx-5" src={{ asset('img/logo.png') }} alt="Logo">
		<ul class="navbar-nav col-10 flex">
			<li class="nav-item mx-5">
				<a class="nav-link active text-light" aria-current="page" href={{ path('app_home') }} title="Accueil">Accueil</a>
			</li>
			<li class="nav-item dropdown mx-5">
				<a class="nav-link dropdown-toggle text-light" href="#" title="Prestations" role="button" data-bs-toggle="dropdown" aria-expanded="false">
					Prestations
				</a>
				<ul class="dropdown-menu">
					<li>
						<a class="dropdown-item" href="{{path('app_prestations')}}">Tous les massages</a>
					</li>
					<li><hr class="dropdown-divider"></li>
					{% for massage in massages %}
						<li>
							<a class="dropdown-item" href="{{path('app_prestation', {'id': massage.id})}}">{{massage.titre|upper}}</a>
						</li>
					{% endfor %}
				</ul>
			</li>
			<li class="nav-item mx-5">
				<a class="nav-link text-light" href="{{path('app_articles')}}" title="Nouveautés">Nouveautés / Articles</a>
			</li>
			<li class="nav-item mx-5">
				<a class="nav-link text-light" href={{ path('app_contact') }} title="Contact">Contact</a>
			</li>
			{% if app.user %}
				<li class="nav-item dropdown mx-5">
					<a class="nav-link dropdown-toggle text-light" href="#" title="Compte" role="button" data-bs-toggle="dropdown" aria-expanded="false">
						<i class="fa-solid fa-user fa-lg"></i>
					</a>
					<ul class="dropdown-menu">
						<li>
							<a class="dropdown-item" href="{{path('app_profil')}}">Mon Profil</a>
						</li>
						{% if is_granted('ROLE_ADMIN') %}
							<li><hr class="dropdown-divider"></li>
							<li>
								<a class="dropdown-item" href="{{path('app_massage_index')}}">Gestion Prestations</a>
							</li>
							<li>
								<a class="dropdown-item" href="{{ path('app_article_index') }}">Gestion Articles</a>
							</li>
							<li>
								<a class="dropdown-item" href="{{ path('app_user_index') }}">Gestion Membres</a>
							</li>
							<li>
								<a class="dropdown-item" href="{{path('app_slider_index')}}">Gestion Images d'Accueil</a>
							</li>
						{% else %}
							<li>
								<a class="dropdown-item" href="#">Mes rendez-vous</a>
							</li>
						{% endif %}
						<li><hr class="dropdown-divider"></li>
						<li>
							<a class="dropdown-item" href="{{path('app_logout')}}">Déconnexion</a>
						</li>
					</ul>
				</li>
			{% else %}
				<ul id="compte" class="d-flex align-items-center mx-5 navbar-nav">
					<li class="nav-item mx-2">
						<a class="nav-link text-light" href={{ path('app_register') }} title="Inscription">Inscription</a>
					</li>
					<li class="nav-item mx-2">
						<a class="nav-link text-light" href={{ path('app_login') }} title="Connexion">Connexion</a>
					</li>
				</ul>
			{% endif %}
		</ul>
	</div>
</nav>
